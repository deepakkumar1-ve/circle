version: 2
jobs:
  one:
    docker:
      - image: circleci/node:10.16.0
    steps:
      - checkout
      - run: echo "A First Hello"
  two:
    docker:
      - image: circleci/node:10.16.0
    steps:
      - checkout
      - run: echo "Second"

workflows:
  version: 2
  one_and_two:
    jobs:
      - one
      - two
